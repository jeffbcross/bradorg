import React from 'react';
import { Button } from '@bradorg/components';

import './app.scss';

export const App = () => {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./${fileName}.${style} file.
   */
  return <Button></Button>;
};

export default App;
